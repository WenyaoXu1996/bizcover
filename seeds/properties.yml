version: 2

seeds:
  - name: BIRD_STRIKES_CURATION
    config:
      column_types:
        record_id: integer(4)
        aircraft_type: varchar(5000)
        airport_name: varchar(5000)
        altitude_bin: varchar(5000)
        aircraft_model: varchar(5000)
        wildlife_struck: varchar(5000)
        wildlife_struck_actual: integer(4)
        flight_impact: varchar(5000)
        flight_date: varchar(5000)
        indicated_damage: varchar(5000)
        airline_operator: varchar(5000)
        origin_state: varchar(5000)
        flight_phase: varchar(5000)
        precipitation: varchar(5000)
        wildlife_collected: bool
        wildlife_sent: bool
        remarks: varchar(5000)
        wildlife_size: varchar(5000)
        sky_condition: varchar(5000)
        wildlife_species: varchar(5000)
        pilot_warned: varchar(5000)
        total_cost: varchar(5000)
        ground_feet: varchar(5000)
        people_injured: integer(4)
        large_aircraft: varchar(5000)
        aircraft_engines: varchar(5000)
        filename: varchar(5000)